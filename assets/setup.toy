InitScreen(800, 600, "Bottle Battle");
//InitLoop(null, null, null);

//check the terrain object works
var terrain = CreateTerrain(40, 40);

print terrain;
print terrain.width;
print terrain.height;

terrain.setTile(20, 30, 42);
print terrain.getTile(20, 30);

UnloadTerrain(terrain);
print terrain; //should print 'null'