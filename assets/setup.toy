srand(time());

InitScreen(50 * 16, 50 * 16, "Bottle Battle");
//InitLoop(null, null, null);

//load the terrain object
var terrain = LoadTerrain();

//if (terrain == null) {
	import "terrain_procgen.toy" as procgen;
	terrain = procgen()(50, 50, 20);
	SaveTerrain(terrain);
//}


SetTerrainLockState(true);