srand(time());

InitScreen(1080, 720, "Bottle Battle");
//InitLoop(null, null, null);

import "terrain_default.toy" as terrain_default;

var terrain = terrain_default();
//SaveTerrain(terrain);

SetTerrainLockState(true);